#include <bits/stdc++.h> 

typedef long long ll;
typedef std::vector<int> vi;
typedef std::pair<int,int> ii;

using namespace std;


int main(){
    
    int n;
    set<set<int>> x;
    while(cin >> n){
        if (n == 0) break;
        x.clear();
        for(int j =0;j<n;j++){
            set<int> s;
            for (int i=0;i<5;i++){
                int t; cin >>t;set.insert(t);
            }   
            if (x.find(s) != x.e;nd()){
            }
        }
    }

    return 0;
}
